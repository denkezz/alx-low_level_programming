#include <stdio.h>
/**
 * main - entry point
 * Description: print numbers
 * Return: always 0(success)
 */
int main(void)
{
int i;
for (i = 0; i <= 0; i++)
{
printf("%i", i);
}
printf("\n");
return (0);
}
